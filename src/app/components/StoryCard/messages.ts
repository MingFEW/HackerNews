import { translations } from '@/locales/translations'
import { _t } from '@/utils/messages'

export const messages = {
  postedBy: () => _t(translations.postedBy),
  totalComments: () => _t(translations.totalCommments)
}
